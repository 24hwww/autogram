## Packages
framer-motion | For smooth layout animations and interactions
date-fns | For date formatting and manipulation

## Notes
Images are served from /generated_images/*.png as returned by the API
Status workflow: pending -> scheduled -> published (or failed)
Rate limiting logic is handled by the backend, frontend should show remaining quota
